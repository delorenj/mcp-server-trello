# Trello API Configuration
# Get your API key from https://trello.com/app-key
TRELLO_API_KEY=your-api-key-here

# Generate your token using your API key
TRELLO_TOKEN=your-token-here

# Optional: Initial workspace ID (can be changed later using set_active_workspace)
# TRELLO_WORKSPACE_ID=your-workspace-id-here

# =============================================================================
# Cache Configuration (all values in seconds, defaults shown)
# =============================================================================

# Enable/disable caching (default: true)
# Set to "false" or "0" to disable caching completely
# TRELLO_CACHE_ENABLED=true

# TTL for board metadata (default: 21600 = 6 hours)
# TRELLO_CACHE_TTL_BOARDS=21600

# TTL for lists (default: 3600 = 1 hour)
# TRELLO_CACHE_TTL_LISTS=3600

# TTL for labels (default: 21600 = 6 hours)
# TRELLO_CACHE_TTL_LABELS=21600

# TTL for board members (default: 43200 = 12 hours)
# TRELLO_CACHE_TTL_MEMBERS=43200

# TTL for cards and checklists (default: 120 = 2 minutes)
# TRELLO_CACHE_TTL_CARDS=120

# TTL for recent activity (default: 60 = 1 minute)
# TRELLO_CACHE_TTL_ACTIVITIES=60
