# If your board is `https://trello.com/b/7aABC123A/myorg`
# replace `/myorg` with `.json` like this:
#
# https://trello.com/b/7aABC123A.json`
#
# The first field (`:id`) in the rendered json block is the board id
TRELLO_BOARD_ID=<YOUR-BOARD-ID>

TRELLO_API_KEY=<YOUR-API-KEY> # https://trello.com/app-key
TRELLO_TOKEN=<YOUR-APP-TOKEN> # https://trello.com/1/authorize?expiration=never&name=YOUR_TRELLO_WORKSPACE&scope=read,write&response_type=token&key=YOUR_API_KEY

# =============================================================================
# CACHING CONFIGURATION
# =============================================================================
# The MCP server includes an intelligent caching layer to reduce API calls
# and improve response times. Caching is enabled by default.

# Enable/disable caching (default: true)
# Set to "false" or "0" to disable
# TRELLO_CACHE_ENABLED=true

# Cache backend: "memory" (default) or "valkey"
# - memory: In-process cache using node-cache (no external dependencies)
# - valkey: Persistent cache using Valkey/Redis (requires running server)
# TRELLO_CACHE_STORE=memory

# Valkey/Redis URL (only used when TRELLO_CACHE_STORE=valkey)
# Valkey is the open-source fork of Redis (BSD-3 license)
# Works with both Valkey and Redis servers
# TRELLO_VALKEY_URL=redis://localhost:6379

# Alternative: Use TRELLO_REDIS_URL if you prefer (same behavior)
# TRELLO_REDIS_URL=redis://localhost:6379

# Connection timeout in milliseconds (default: 5000)
# TRELLO_CACHE_CONNECT_TIMEOUT=5000

# Cache key prefix for namespacing (default: trello-mcp)
# Useful when sharing a Valkey instance with other applications
# TRELLO_CACHE_KEY_PREFIX=trello-mcp

# =============================================================================
# CACHE TTL CONFIGURATION (Time-To-Live in seconds)
# =============================================================================
# Customize how long different types of data are cached.
# Lower values = more fresh data, higher API usage
# Higher values = better performance, potentially stale data

# Board and workspace metadata (default: 21600 = 6 hours)
# TRELLO_CACHE_TTL_BOARDS=21600

# Lists on a board (default: 3600 = 1 hour)
# TRELLO_CACHE_TTL_LISTS=3600

# Labels on a board (default: 21600 = 6 hours)
# TRELLO_CACHE_TTL_LABELS=21600

# Board members (default: 43200 = 12 hours)
# TRELLO_CACHE_TTL_MEMBERS=43200

# Cards, checklists (default: 120 = 2 minutes)
# TRELLO_CACHE_TTL_CARDS=120

# Recent activity (default: 60 = 1 minute)
# TRELLO_CACHE_TTL_ACTIVITIES=60
